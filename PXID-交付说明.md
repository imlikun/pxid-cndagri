# PXID 品向网站首版交付说明

## 交付范围

本目录是从 `/Users/likun/Desktop/cndagri/` 独立复制后改造的 PXID 首版，原目录没有修改。

保留了参考站的页面 Section 数量、图片与文字的空间关系、轮播、波浪转场、滚动入场、上下滚恢复、桌面与手机导航等框架机制；可见业务内容、品牌、图片、链接与页面栏目已替换为 PXID。

## 页面入口

- `cn/index.html`：首页
- `cn/company.html`：关于 PXID
- `cn/business.html`：产品方案与制造能力
- `cn/sustainability.html`：ODM 全流程
- `cn/news.html`：洞察与资料
- `cn/contact.html`：启动项目
- `cn/privacy-policy.html`：隐私政策占位页
- `cn/legal-notice.html`：法律声明占位页
- `cn/news/`：4 篇项目方法与工程制造文章

根目录同名旧入口已改为中文 PXID 页面跳转；原农业新闻路径不再显示旧内容，统一回到 PXID 洞察页。

## 中国地图模块

- 保留首页原 R5 地图型叙事位置。
- 以淮安为唯一明确地理起点。
- 7 条发射路径连接产品定义、工业设计、结构工程、电控开发、工程样车、制造装配和试验交付。
- 线路按顺序绘制，端点随后出现；向上离开后复位，再次下滚可重新播放。
- 手机端保留地图主体，能力说明改为可横向浏览，避免文字压在地图上。
- `?motion=reduce` 可预览减少动态效果降级，系统设置 `prefers-reduced-motion: reduce` 时也会自动启用同类降级。

当前地图用于结构与动效评审。正式公开前必须换成通过审图的中国地图素材，并由 PXID 核准需要表达的真实业务节点。现有端点只代表能力路径，不表示城市客户、分公司或市场覆盖。

## 双击打开

优先双击站点根目录中的：

```text
打开PXID网站.command
```

它会自动启动本地预览并打开浏览器。预览期间请保留弹出的终端窗口，关闭该窗口后预览服务自动结束。

`cn/index.html` 也已改为普通脚本并增加加载卡死兜底，可以直接双击；但不同浏览器对 `file://` 的视频、字体和脚本策略并不完全一致，因此正式评审仍建议使用上述启动器。

## 手动本地预览

建议在本目录启动静态服务器：

```bash
python3 -m http.server 4174 --bind 127.0.0.1
```

然后打开：

```text
http://127.0.0.1:4174/cn/index.html
```

## 已完成验证

- 31 个中文页面与兼容跳转：0 个缺失本地引用。
- 12 个正式内容页面：图片全部成功加载、横向溢出为 0。
- 桌面 1440 × 900、手机 390 × 844：首页、菜单、地图、产品网格和联系表单已实际检查。
- 地图正常下滚、向上恢复、再次进入、减少动态效果模式已检查。
- 新标签页逐页检查未发现 JavaScript 运行错误。
- 联系表单只做本地校验，不会向任何服务器发送资料。

## 正式上线前仍需确认

1. 中国地图的审图版素材与批准节点。
2. 隐私政策、法律声明的正式法务文本。
3. 表单接收接口、通知对象与隐私授权文案。
4. 电话、邮箱、地址与所有产品名称的最终业务核准。
5. 是否建设独立英文站；当前 EN 入口保留在中文站。
6. 参考框架现有的在线字体与图标依赖是否在部署前完全本地化。
